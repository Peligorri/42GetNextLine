# 42GetNextLine
This project is the first one I completed in Milestone 1 of the 42 cursus. In it, I had to create a C program that reads a document line by line using my libft library, along with the functions `read`, `malloc`, and `free`.
